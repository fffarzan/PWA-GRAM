{
  "name": "pwa-gram",
  "version": "1.0.0",
  "description": "A PWA sample app",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json & http-server -c-1"
  },
  "author": "fffarzan",
  "license": "ISC",
  "devDependencies": {
    "http-server": "^0.9.0",
    "json-server": "^0.17.0"
  }
}
